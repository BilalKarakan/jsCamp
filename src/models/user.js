export default class User {
    constructor(id, firstName, newFirstName, city) {
        // console.log("user oluştu.")
        this.id = id
        this.firstName = firstName
        this.newFirstName = newFirstName
        this.city = city
    }
}